spring:
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: "jdbc:postgresql://localhost:5432/weather_archive"
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
